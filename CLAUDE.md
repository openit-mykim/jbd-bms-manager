# Coding Agent Instructions

This file exists for coding agents that automatically look for `CLAUDE.md`.

The authoritative repository instructions are in `AGENTS.md`.

Before making changes:

1. Read `AGENTS.md` completely.
2. Read `PROJECT_STATUS.md` to identify the current phase and next task.
3. Read the relevant documents under `docs/`.
4. Use an isolated Paseo worktree for non-trivial changes.
5. Run focused verification and report evidence.

Do not duplicate or override the rules in `AGENTS.md`. If this file and `AGENTS.md` differ, `AGENTS.md` wins.
